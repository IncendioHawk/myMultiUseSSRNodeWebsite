<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Confirm Delete</title>
    <link rel="stylesheet" href="/stylesheets/main.css" />
  </head>

  <body>
    <div class="login-form-container">
      <h1 style="font-size: larger">Sign in to confirm account details</h1>
      <form class="login-form" method="post" action="/delete">
        <div class="username-wrapper user-input">
          <label for="username">Username</label>
          <input
            placeholder
            type="text"
            name="username"
            id="username"
            required
            value="<%= locals.username %>"
            autocapitalize="none" />
          <% if (locals.error && locals.usernameMessage != null) {%>
          <div class="error error-email"><%= locals.usernameMessage %></div>
          <% } %>
        </div>
        <div class="password-wrapper user-input">
          <label for="password">Password</label>
          <input
            placeholder
            type="password"
            name="password"
            id="password"
            required
            autocapitalize="none" />
          <% if (locals.error && locals.passwordMessage != null) {%>
          <div class="error error-password"><%= locals.passwordMessage %></div>
          <% } %>
        </div>
        <input class="submit-btn" value="Delete" type="submit" />
        <div class="signup-link-container">
          <a class="signup-link" href="/">Return Home</a>
        </div>
      </form>
    </div>
  </body>
</html>
